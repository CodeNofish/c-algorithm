#include <iostream>
#include <algorithm>
using namespace std;

#define N 10004
#define M 100008

struct Edge {
    int from,to,next;
};
Edge edges[M];
int cnt = 0;
void add_edge(int x, int y) {
    edges[++cnt].next = head
}


int
main()
{
    int n,m;
    int a, u, v;
    cin >> n >> m;
    for (int i = 0; i < n; ++i) {
        cin >> a;
    }
    for (int i = 0; i < m; ++i) {
        cin >> u >> v;
    }
}